services:
  mongodb:
    image: mongo:7
    container_name: docanalytics_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: document_analytics_db

volumes:
  mongo_data:
