"""App configuration for analytics_engine."""

from django.apps import AppConfig


class AnalyticsEngineConfig(AppConfig):
    """Django AppConfig for the analytics engine."""

    name = 'analytics_engine'
